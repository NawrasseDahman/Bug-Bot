"use strict";
const config = require("../config");
let utils = require("./utils");

let attachUtils = {
  attach: function(reportStringSubmit, header, cleanRepostReport, channelID, userTag, msgID, os, listID, userID, submitReport) {

  }
}

module.exports = attachUtils;
